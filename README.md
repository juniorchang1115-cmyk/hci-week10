# Sparring AI - Backend Handoff Bundle

Hello Gemini CLI,

You are assigned to build the backend for this Sparring AI landing page.

## Instructions

1. **Analyze the Project**: Read the complete directory structure and understand the existing frontend project.
2. **Backend Only**: You are ONLY tasked with creating the backend. Use a local JSON file or SQLite to store the form data. **DO NOT** use any external database.
3. **Frontend Integration**: On the React form component inside `public/index.html` (the landing page), you must **ONLY** add an `onSubmit` handler, `state`, and `fetch` to connect to the new API.
4. **Strict Visual Preservation**: **DO NOT** modify any HTML markup, styles, layout, or class names.
5. **Preserve Visuals**: Retain all existing frontend visuals perfectly as they are.

## Goal

Ensure the landing page can run on `localhost`. I should be able to open my browser, fill out the form, submit it, and the backend should successfully receive and store the data.
